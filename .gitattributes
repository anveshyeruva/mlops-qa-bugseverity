# Normalize line endings across platforms
* text=auto

# ---- Code & scripts (force LF on scripts for portability)
*.sh text eol=lf
*.bash text eol=lf
*.py text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.toml text eol=lf
*.json text eol=lf
*.md text eol=lf

# ---- Make CSV/TSV diffs readable in GitHub UI
*.csv text diff
*.tsv text diff

# ---- Treat notebooks as text so GitHub renders rich diffs
# (GitHub already supports .ipynb diffing)
*.ipynb text

# ---- Binary/large artifacts: don't attempt textual diffs
*.joblib binary
*.pkl    binary
*.bin    binary
*.parquet binary
*.arrow  binary
*.onnx   binary
*.pt     binary
*.pth    binary
*.tflite binary
*.h5     binary
*.hdf5   binary
*.npz    binary
*.npy    binary
*.gz     binary
*.zip    binary
*.7z     binary
*.tar    binary
*.pdf    binary
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.bmp    binary
*.ico    binary
*.svg    text  # SVG is XML; allow diffs

# ---- Documentation/media classification for GitHub Linguist
docs/**                linguist-documentation
docs/images/**         linguist-documentation
.github/**             linguist-documentation

# ---- Generated / vendored content (minimize language stats noise)
mlruns/**              linguist-generated
models/**              linguist-generated

# Optional: mark dependencies/vendor dirs as vendored if you ever add them
# vendor/**             linguist-vendored
# third_party/**        linguist-vendored

# ------------------------------
# OPTIONAL: Git LFS for big files
# Uncomment these lines IF you enable Git LFS (git lfs install)
# *.parquet filter=lfs diff=lfs merge=lfs -text
# *.joblib  filter=lfs diff=lfs merge=lfs -text
# *.pkl     filter=lfs diff=lfs merge=lfs -text
# *.pt      filter=lfs diff=lfs merge=lfs -text
# *.h5      filter=lfs diff=lfs merge=lfs -text
# *.onnx    filter=lfs diff=lfs merge=lfs -text
# *.pdf     filter=lfs diff=lfs merge=lfs -text
# *.png     filter=lfs diff=lfs merge=lfs -text
# *.jpg     filter=lfs diff=lfs merge=lfs -text
# ------------------------------

